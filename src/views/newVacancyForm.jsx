const React = require('react');
const Layout = require('./Layout');
const MainLayout = require('./MainLayout');

module.exports = function newVacancyForm({ newUser }) {
  return (
    <MainLayout>

    </MainLayout>
  )
};